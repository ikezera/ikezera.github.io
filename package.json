{
  "name": "ikezera-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test-local": "pushd dist && python3 -m http.server 8000 && popd"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  }
}
